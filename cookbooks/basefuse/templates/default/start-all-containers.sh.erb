#!/bin/sh
#
# Script for starting all the Fuse containers.
# This file was generated by the Chef 'basefuse' cookbook (updated <%= Time.now.utc.strftime('%d %b %Y %H:%M') %>)
#
# Prerequisites:
# - must be run as fuse
#
# Usage: start-all-containers.sh
#
# Author: John Topley
#

<%= @fuse_bin_dir %>/client 'fabric:container-list' | awk 'NR>2 { print $1 }' | xargs -n 1 <%= @fuse_bin_dir %>/client 'fabric:container-start'
