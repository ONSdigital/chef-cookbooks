# This file was generated by the Chef 'baseservicemix' cookbook (updated <%= Time.now.utc.strftime('%d %b %Y %H:%M') %>)
#
# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
. ~/.bashrc
fi

# User specific environment and startup programs
PATH=/usr/local/bin:/bin:/usr/bin:/usr/local/sbin:/usr/sbin:/sbin:<%= @servicemix_bin_dir %>:<%= @home_dir %>/bin

export PATH
export JAVA_HOME=/usr/lib/jvm/jre
